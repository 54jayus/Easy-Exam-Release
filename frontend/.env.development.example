# Python 运行环境已统一迁移到仓库根目录的 .env.runtime.local。
# frontend/.env.development 不再承担 Python 路径配置职责。
#
# 如需配置本机运行环境：
# 1. 复制仓库根目录的 .env.runtime.example 为 .env.runtime.local
# 2. 在 .env.runtime.local 中填写 conda 环境名或 Python 可执行命令
