# 前端设计要求（现行）

本文档记录当前仓库前端界面的现行设计方向，用于后续维护、扩展和局部改版时保持一致性。它不是一份未来重构提案，也不是提示词，而是对当前实现风格的约束说明。

## 1. 总体方向

当前前端采用偏现代化工具软件的视觉语言，重点是：

- 左侧沉浸式导航
- 顶部信息栏与流程进度提示
- 浅色主界面
- 明确的模块分区
- 适度动画而非堆砌动效

整体目标不是做营销站风格，而是让考试工作流清楚、稳定、可快速上手。

## 2. 视觉基调

当前代码中的主色和基础色来自：

- `frontend/src/styles.css`
- `frontend/tailwind.config.js`

现行颜色方向：

- 主色：`Indigo`
- 背景：`Slate / Surface`
- 功能色：
  - 科目相关偏 `Sky`
  - 监考相关偏 `Emerald`
  - 警告与错误偏 `Rose`

现行字体方向：

- 优先使用 `Inter`
- 回退到系统无衬线字体

维护要求：

- 不要回退到 Element Plus 默认视觉风格
- 页面新增样式时优先复用现有主色、背景色和圆角尺度
- 主题变量变更时同步检查 `styles.css`、`tailwind.config.js` 和页面局部覆盖样式

## 3. 布局要求

当前全局框架由 `frontend/src/App.vue` 定义，后续页面应默认继承这套布局：

- 左侧固定导航栏
- 右侧主内容区
- 顶部标题栏
- 工作流页面展示步骤指示器
- 内容区内部滚动

维护要求：

- 不要绕开 `App.vue` 自行创建第二套全局导航体系
- 新页面优先接入现有导航与标题机制
- 工作流相关页面尽量保持与 `Subjects -> Proctoring -> Rooms -> Printing` 一致的进入节奏

## 4. 页面设计要求

### 4.1 Dashboard

应突出：

- 当前系统状态
- 工作流完成度
- 模块摘要信息

### 4.2 Subjects / Proctoring / Rooms / Printing

这些页面属于主业务流，界面应强调：

- 操作顺序清晰
- 表单与结果区域分层明确
- 重要结果可被快速确认
- 错误、警告和导出动作有明显反馈

### 4.3 Help

帮助中心应强调：

- 内容可读性
- 目录导航
- 搜索可达性
- 长文档滚动定位

## 5. 动效要求

当前项目已经使用了轻量级转场和进入动画，后续应保持同一原则：

- 动效要服务于层级变化和状态反馈
- 避免大幅、频繁、装饰性过强的动画
- 尊重 `prefers-reduced-motion`

维护要求：

- 全局或页面级动画优先沿用现有淡入、位移、对话框切换模式
- 不要为普通按钮和普通列表随意叠加重动效

## 6. 组件与实现要求

当前项目的实现策略是：

- Element Plus 提供基础组件能力
- Tailwind 负责布局、间距和快速样式组织
- 页面特有复杂逻辑拆到 composables
- 页面局部弹窗和复杂片段拆到页面同名目录内

维护要求：

- 复杂页面不要继续向单个 `.vue` 文件堆逻辑
- 新增公共样式优先抽到全局或可复用层
- 页面专属样式和组件优先留在该页面目录中

## 7. 响应式与可用性

虽然当前应用以桌面端为主，但仍应保持：

- 常见窗口尺寸下可正常使用
- 文本不会在主要区域大量溢出
- 弹窗在中等尺寸窗口下仍可操作
- 表格、抽屉和预览区在较窄宽度下仍能阅读

## 8. 文档维护规则

如果后续视觉方向发生明显变化，应同时更新：

- 本文件
- `frontend/src/styles.css`
- `frontend/tailwind.config.js`
- `frontend/src/App.vue`

历史设计提案和一次性重构提示词，不应继续留在活跃文档里充当现行规范。
