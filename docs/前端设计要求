Role: 你是一位拥有10年经验的 B 端产品 UI/UX 设计师及前端架构师，擅长将复杂的工具软件重构为现代化的 SaaS 风格应用。

Project Context: 这是一个名为“智能考务系统”的桌面端应用（基于 Vue3 + Element Plus）。 核心业务流：用户通常按照 科目设置 -> 监考编排 -> 考场编排 -> 资料打印 的顺序进行操作。 当前痛点：界面过于传统（像旧时代的管理后台），缺乏引导性，视觉沉闷，动效缺失。

Design Goal: 请为该项目设计一套全新的 UI/UX 规范和页面布局方案。风格要求：现代化、专业、高效、流畅。

1. 视觉风格定义 (Visual Style)
设计语言：采用 "Clean SaaS" 风格。结合 Neo-Brutalism (新粗野主义) 的轻微触感（清晰的边框、高对比度）与 Glassmorphism (磨砂玻璃) 的细腻质感。
配色方案：
主色 (Primary)：#4F46E5 (Indigo 600) - 代表智慧与专业。
背景色：#F8FAFC (Slate 50) - 避免纯白刺眼，增加纸质感。
功能色：
科目/考试：#0EA5E9 (Sky Blue)
监考/教师：#10B981 (Emerald)
冲突/警告：#F43F5E (Rose)
字体与排版：使用 Inter 或系统默认无衬线字体，强调数字（如考场数、时间）的 Font Weight，提升数据可读性。
2. 核心布局与导航 (Layout & Navigation)
导航重构：
由于业务有强顺序性，请设计一个 "左侧沉浸式导航栏"。
在导航栏顶部增加一个 "流程进度指示器" (Step Wizard)，高亮当前所处的考试筹备阶段（如：当前在第2步“监考编排”）。
Dashboard (首页)：
设计一个“考务指挥舱”。展示当前考试的倒计时、核心数据概览（科目数、监考人数、考场数），以及“快捷入口”卡片。
3. 关键页面重设计 (Key Modules Redesign)
A. 科目管理 (Subject Settings)

形态：抛弃传统表格。采用 "Bento Grid (便当盒)" 卡片布局。
交互：每个科目卡片显示时间轴（考试起止时间）。支持拖拽排序。
动效：卡片悬停时轻微上浮（TranslateY -4px）并加深阴影。
B. 监考编排 (Proctor Scheduling)

形态：分屏设计。左侧为“资源池”（教师列表），右侧为“排班表”（日历视图或甘特图视图）。
交互：支持从左侧拖拽教师到右侧考场进行手动调整。
视觉：使用不同颜色的 Tag 区分“本校/外校”教师。
C. 考场编排 (Exam Room Scheduling)

核心亮点：可视化。设计一个虚拟的 "考场座位地图"。
交互：点击“开始编排”时，展示一个洗牌/发牌的动画效果，模拟学生被随机分配到座位的过程，增加系统的“智能感”。
D. 资料打印 (Material Printing)

形态："所见即所得 (WYSIWYG)"。左侧为配置面板（数据源、格式），右侧为实时的 A4 纸张预览区。
交互：切换“台角纸”或“准考证”时，右侧预览区通过 Flip (翻转) 或 Fade (淡入淡出) 动画平滑切换模板样式。
4. 动效与微交互 (Motion & Micro-interactions)
技术栈建议：推荐引入 @vueuse/motion 或 AutoAnimate。
页面转场：路由切换时，内容区域使用 Slide Up + Fade In (从下往上浮出)。
数据加载：使用 Skeleton (骨架屏) 代替转圈 Loading，骨架屏带有流光（Shimmer）效果。
按钮反馈：所有主要操作按钮（如“生成”、“导出”）点击时带有 Scale (缩放) 效果，完成后显示绿色的 Check 动画。
5. 代码实现建议 (Technical Guidelines)
Element Plus 定制：不要直接使用默认样式。通过覆盖 CSS 变量 (--el-color-primary, --el-border-radius-base设为 12px) 来统一风格。
Tailwind CSS：建议使用 Tailwind 处理布局和间距，保持代码整洁。